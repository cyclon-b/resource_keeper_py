import { GLOBAL_ROUTES } from '../assets/config/routes';

export const environment = {
    production: true,
    baseUrl: 'http://localhost:8000',
    baseApi: 'http://localhost:8000/api/',
    baseRoutes: GLOBAL_ROUTES,
};
